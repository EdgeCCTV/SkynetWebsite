# Skynet Monitoring — context

## What this project is

Marketing site for Skynet Monitoring (virtual guard / remote video monitoring). Repo: `EdgeCCTV/SkynetWebsite`. Vercel project `skynet-website` (Edge profile `robertyoung-6171`). Intended public URL: `https://skynetmonitoring.com/`.

## DNS (do not break email)

Squarespace Domains (Google nameservers `ns-cloud-d*.googledomains.com`). Google Workspace MX and the `gv-*.googlehosted.com` CNAMEs must stay.

Website records Vercel asked for (24 Sep 2026), once edited in Squarespace DNS Settings:

- `A` `@` → `76.76.21.21` (replace `141.193.213.20`)
- `A` `www` → `76.76.21.21` (remove the `www` CNAME to `pnofb8l8kmx9.wpeproxy.com` first; a name cannot have both)

There is a second apex-looking `A` record (`141.193.213.21`) in the Squarespace UI. Public DNS only answers `141.193.213.20` for the apex. Confirm that row’s full host name before deleting it. Leave `_domainconnect` alone.

Domains are already added on the Vercel project. Do not move nameservers to Vercel; that would drop Google email.

## Pages

Homepage is edited in `index.html`. Inner pages are generated by `scripts/build-pages.mjs` (`node scripts/build-pages.mjs`). Edit that script, then rerun it. Do not hand-edit the generated HTML or the next run will overwrite it.

Sitemap lists the homepage plus the service, industry, and about URLs.

## SEO decisions

- Canonical host is `https://skynetmonitoring.com/` (trailing slash).
- Homepage query: virtual guard monitoring in Atlanta.
- Schema is `ProfessionalService` plus `Service` items. Do not put `serviceType` on the business. `sameAs` is omitted until Skynet has its own Google Business Profile or social URLs. Footer links to Edge CCTV and Security Camera Atlanta stay ordinary links.
- Stats on the homepage are not headings.
- No Google Business Profile for Skynet was found at this address (Edge CCTV already lists 2470 Windy Hill). Do not create a second listing without Robert confirming the name, phone, and category.
